RadioComponentの映像のとこNullにしないとカメラ自体読み込んでね？
FunctionAppはどこで使われてるか書いておいた方が楽
id系はConstructerにまとめたい

すぐ使えるようにおいておく
const waitAuth:any =
         (() => 
            new Promise((resolve:any,reject:any) => {
                let count = 0;
                setInterval(() => {
                count++;
                if(this.authID !== ""){
                    resolve();
                }else if(count > 20){
                    reject();
                }
                },100);
            })
        )();

        waitAuth.then(() => {
            const CommentRef = ref(db, `RoomBase/${this.authID}`);
            onChildAdded(CommentRef, (snapshot) => {
                this.chatSnapShot.push( {
                    "key":snapshot.key,
                    "kinds":snapshot.val().kinds,
                    "name":snapshot.val().name,
                    "Comment":snapshot.val().message})
                });
        },() => {
            alert("AuthIDが取得できませんでした");
        });


初回起動時ログイン判定おかしくない？（ログインしてるのにページ内読み込み（F5じゃなくてページ遷移）しないと判定されてない）
名前の命名やりながら見直し
配信ページとかその人の配信の視聴ページとか、ページを分ける（生成する）仕組み調べる
んでそれらをランダム表示する仕組み作る
Twitter認証作りたいけどお金かかるようになるらしいな・・・。
マイページ作らなきゃ
フォローの概念作らなきゃ
非表示、ブロック機能作らなきゃ


NGコメントで内容チェック入れる機能作りたい
今の見た目がおもちゃっぽいから何かしら改善したい


コメント欄、お便り対応
→考える
DOM操作じゃなくてDOM切り替えに
→お題機能含む
→考える
ミュート実装
→Agora
ラジオ切り替え
→Agora＆DOM切り替え
ここまでやればリリース自体はできる
けど、足りないものが多すぎるのでこのタイミングで相談入れる